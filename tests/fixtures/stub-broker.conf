# Stand-in for the broker. Answers everything so tests can distinguish
# "request reached the broker" from "gateway denied it" without real creds.
server {
  listen 8080;
  location / {
    return 200 "BROKER-HIT $request_uri\n";
    add_header Content-Type text/plain;
  }
}
