# HAZARD FIXTURE — do not copy. Prefix match instead of exact match.
# tests/10-cred-gateway.test.sh asserts this leaks /github/token, which is
# why gateway.d/README.md mandates `location = /exact/path`.
location /github/ {
  proxy_pass http://broker:8080;
}
